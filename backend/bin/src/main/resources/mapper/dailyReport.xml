<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.itda.mapper.DailyReportMapper">

	<select id="getReportList" parameterType="map" resultType="DailyReport">
		select *
		from dailyReport
		where 
		userId = #{userId}
		and
		reportDate like concat(#{curMonth}, '%')
	</select>
	
	<select id="getReport" parameterType="map" resultType="DailyReport">
		select *
		from dailyReport
		where 
		userId = #{userId}
		and
		reportDate like #{curDay}
	</select>
	
	<select id="getExercise" parameterType="map" resultType="DailyReport">
		select exerciseId
		from dailyReport
		where 
		userId = #{userId}
		and
		reportDate like #{curDay}
	</select>
	
	<select id="getQuestion" parameterType="map" resultType="DailyReport">
		select question1Id
		from dailyReport
		where 
		userId = #{userId}
		and
		reportDate like #{curDay}
	</select>
	
</mapper>