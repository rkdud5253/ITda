<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.itda.model.mapper.ExerciseMapper">

	<select id="getExercise" parameterType="String" resultType="Exercise">
	select exercise_id, exercise_name, exercise_url
	from exercise
	</select>
	
	<update id="updateAccuracy" parameterType="Report">
	update report
	set exercise_id = #{exerciseId},
	exercise_accuracy = #{exerciseAccuracy}
	where user_id = #{userId}
	and
	report_date = #{reportDate}
	</update>

</mapper>